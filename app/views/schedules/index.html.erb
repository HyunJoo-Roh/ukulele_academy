<h1>레슨 스케쥴 현황</h1>

<!-- table>
  <thead>
    <tr>
      <th>날짜</th>
      <th>시간</th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody> -->
    <% @week.each do |w| %>
      <% w.each do |s| %>
       <%= s.day %>
      <% end %> 
    <!-- <div class="monday"><%#= link_to schedule.day, edit_schedule_path(schedule) %></div>
    <div class="tuesday"></div>
    <div class="wednesday"></div>
    <div class="thursday"></div>
    <div class="friday"></div>
    <div class="saturday"></div> -->
      <!-- <tr>
        <td></td>
        <td><%#= schedule.hour %></td>
        <td><%#= link_to schedule.user.name, user_lessons_path(schedule.user) %></td>
        <td><%#= link_to '수정', edit_schedule_path(schedule) %></td>
        <td><%#= link_to '삭제', schedule, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr> -->
    <% end %>
  <!-- </tbody>
</table> -->

<br>

<%= link_to '새 일정 등록', new_schedule_path %>
