<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name, '학생이름' %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :phone, '전화번호' %><br>
    <%= f.text_field :phone %>
  </div>
  <div class="field">
    <%= f.label :email, '이메일' %><br>
    <%= f.text_field :email %>
  </div>
  <div class="actions">
   <%= f.submit("새 학생 등록",{class: "btn btn-success pull-left"}) %>
  </div>
<% end %>
